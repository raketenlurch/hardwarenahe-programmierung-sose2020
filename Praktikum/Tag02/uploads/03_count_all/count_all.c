/* count_all.c */
#include <stdio.h>
#include <string.h>
#include "count_all.h"

char tokens[80][81];

// Anzahl der Tokens allgemein herausfinden
// Tokens sortieren
// Anzahl der verschiedenen Tokens ausgeben
int extract_and_count(char *source, int *count) {
  while (source[i] != '\0') {
    if ()
  }
}

void print(int diff_tokens, int *count) {

}
